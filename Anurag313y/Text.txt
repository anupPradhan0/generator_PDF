Stack breakdown:

MongoDB — persistent volume via docker-compose
Express + Node — REST API in ./backend (auth, events CRUD, PDF export)
React — dashboard UI in ./frontend (events form: name, mobile, event name, date)

Project root: Anurag313y/

Run locally: backend + frontend npm dev, OR `docker compose up --build` (see DOCKER.md).
